import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';  // Global import
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
